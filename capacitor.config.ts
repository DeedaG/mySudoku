import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.christigames.sharpgames',
  appName: 'Christi Games',
  webDir: 'dist/mySudoku/browser'
};

export default config;


